import {
  twoSum,
  // twoSumBruteForce as twoSum,
  //twoSumTwoPassHashes as twoSum
} from './167-2sum-problem-sorted';

describe('two sum test case', () => {
  xit('empty array', () => {;
    expect(twoSum([], 9)).toEqual(-1);
  });

  // it('there are indexes', () => {

  // });
});
